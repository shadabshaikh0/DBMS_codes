create table student (
	rollno number,
	name varchar2(30),
	dateofadmission date,
	branch varchar2(30),
	percentage number,
	status varchar2(30)
);

create table alumni (
	rollno number,
	name varchar2(30),
	dateofadmission date,
	branch varchar2(30),
	percentage number,
	status varchar2(30)
);
